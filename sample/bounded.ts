function createAccount(hatac: true) {
    // by this point, T&C must have been accepted!
}

// createAccount(false);

const onlyTrue: true = true
const mustBeFive: 5 = 5
const fooInBar: { 'bar': { 'foo': 1 } } = { 'bar': { 'foo': 1 } }